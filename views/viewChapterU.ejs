<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Chapter Pages</title>
    <!-- Include Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">

    <div class="container mx-auto py-8 px-4">

        <h1 class="text-3xl font-semibold mb-4">Chapter Title: <%= name %>
        </h1>
        <h2 class="text-xl mb-4">Pages:</h2>

        <ul id="pagesList" class="space-y-4">
            <% page.forEach((p)=> { %>
                <li>
                    <div class="flex items-center py-2 px-4 bg-white rounded-lg shadow-md hover:bg-purple-50">
                        <form action="/viewPage" method="get" class="flex items-center">
                            <input type="hidden" id="head" name="head" value="<%= p.head %>">
                            <input type="hidden" id="body" name="body" value="<%= p.body %>">
                            <input type="hidden" id="chapter" name="chapter" value="<%= name %>">
                            <input type="hidden" id="courseId" name="courseId" value="<%= courseId %>">
                            <button type="submit" class="text-blue-500 hover:text-blue-700 mr-2">
                                <%= p.head %>
                            </button>
                            <% if (p.completed) { %>
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 ml-2 text-green-500"
                                    viewBox="0 0 20 20" fill="currentColor">
                                    <path fill-rule="evenodd"
                                        d="M19.707 2.293a1 1 0 00-1.414 0l-9 9a1 1 0 01-1.414 0l-4-4a1 1 0 00-1.414 1.414l4.293 4.293a1 1 0 001.414 0l9-9a1 1 0 000-1.414z"
                                        clip-rule="evenodd" />
                                </svg>
                                <% } %>
                        </form>
                    </div>
                </li>
                <% }); %>
        </ul>

    </div>

</body>

</html>