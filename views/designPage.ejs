<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page Design
    </title>

    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
</head>

<body class="bg-gray-100 p-4">
    <h1 class="text-3xl mb-4">New Page <%=courseId%>
    </h1>

    <form id="designForm" action="/designPage" method="post" class="max-w-lg mx-auto">
        <div class="mb-4">
            <label for="Chapter" class="block">Chapter</label>
            <input type="text" name="Chapter" id="Chapter" value="<%= title %>"
                class="block w-full bg-white border border-gray-300 rounded-md py-2 px-3 focus:outline-none focus:border-blue-500">
        </div>
        <input type="hidden" name="courseId" value="<%=courseId%>">
        <div class="mb-4">
            <label for="Page" class="block">Page Name</label>
            <input type="text" name="Page" id="Page"
                class="block w-full bg-white border border-gray-300 rounded-md py-2 px-3 focus:outline-none focus:border-blue-500">
        </div>
        <div id="editor" class="bg-white p-4 rounded mb-4">
            <h1 class="text-2xl mb-2">Welcome to the Page Editor</h1>
            <div id="quillEditor" style="min-height: 200px;"></div>
            <input type="hidden" id="editorContent" name="editorContent">
        </div>
        <button type="submit"
            class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Submit</button>
    </form>

    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>

    <script>
        var quill = new Quill('#quillEditor', {
            theme: 'snow'
        });

        document.getElementById('designForm').addEventListener('submit', function (event) {
            document.getElementById('editorContent').value = quill.root.innerHTML;
        });
    </script>
</body>

</html>